name: diracx
channels:
  - diracgrid
  - conda-forge
  - nodefaults
dependencies:
  - authlib
  - cachetools
  # Needed because coverage runs inside the image
  # when doing the integration tests
  - coverage
  - email-validator
  - git
  - gitpython
  - httpx
  - pip
  - pydantic =1.10.13
  - python =3.11
  - pyyaml
  - requests
  - urllib3 <2
